Write an optimized Triton kernel in `solution/triton/kernel.py` for maximum speedup on ${GPU_SHORT}.
This is a **{{OPERATOR_DESCRIPTION}}** operator for the MLSys 2026 FlashInfer contest.

The starting point is the **pure-Python reference implementation** (very slow).
Your job is to replace it with a fast Triton kernel while preserving correctness.
