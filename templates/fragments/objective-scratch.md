Write an optimized {{LANGUAGE_NAME}} kernel in `solution/{{LANGUAGE}}/` for maximum speedup on {{GPU_NAME}}.
This is a **{{OPERATOR_DESCRIPTION}}** operator.

**Starting point:**
- Python-like languages (Python, Triton, TileLang): The reference implementation is in `solution/{{LANGUAGE}}/kernel.py`
- Compiled languages (CUDA, C++): Stub templates are in `solution/{{LANGUAGE}}/`. Study `docs/reference.py` for the PyTorch reference implementation.

Replace it with a fast {{LANGUAGE_NAME}} kernel while preserving correctness.
