Write an optimized {{LANGUAGE_NAME}} kernel in `solution/{{LANGUAGE}}/kernel.py` for maximum speedup on {{GPU_NAME}}.
This is a **{{OPERATOR_DESCRIPTION}}** operator.

The starting point is the **pure-Python reference implementation** (very slow).
Replace it with a fast {{LANGUAGE_NAME}} kernel while preserving correctness.
